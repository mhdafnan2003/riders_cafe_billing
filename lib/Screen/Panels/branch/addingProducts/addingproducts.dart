import 'package:flutter/material.dart';

class Addingproducts extends StatelessWidget {
  const Addingproducts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Adding Products'),
    );
  }
}